export type Phase='entry'|'guided_entry'|'parachute'|'powered_descent'|'final_approach'|'touchdown'
export interface Telemetry {mission_time:number;phase:Phase;altitude_m:number;horizontal_velocity_ms:number;vertical_velocity_ms:number;speed_ms:number;mach:number;fuel_fraction:number;heat_flux_kw_m2:number;g_load:number;atmospheric_density_kg_m3:number;downrange_m:number;target_error_m:number;position:{x:number;y:number;z:number};orientation:{pitch:number;yaw:number;roll:number};action:{policy_thrust_command:number;policy_tilt_command:number;applied_throttle:number;applied_tilt_rad:number};forces_n:{gravity:number;drag:number;lift:number;thrust:number};reward:number;cumulative_reward:number}
export interface Report {status:'success'|'failure';failure_reason?:string;guidance_mode:string;landing_error_m:number;touchdown_velocity_ms:number;vertical_velocity_ms:number;fuel_remaining_fraction:number;peak_heat_flux_kw_m2:number;peak_g_load:number;max_mach:number;flight_duration_s:number;cumulative_reward:number}
